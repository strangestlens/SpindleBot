# SpindleBot Python dependencies
#
# Runtime
tomli>=2.0; python_version < "3.11"   # built-in tomllib in 3.11+
mutagen>=1.47                          # FLAC tag reading

# Development / testing
pytest>=7.0
ruff==0.15.20                          # pinned: unpinned ruff drifts and breaks CI on new default rules (e.g. 0.16 I001)
flask>=3.0                             # lrc-editor endpoint tests
