"""SpindleBot's own SQLite database — the system of record for content identity,
the locations a file lives in, and (in later phases) version history. Owned
solely by SpindleBot; never written through `beet`."""
